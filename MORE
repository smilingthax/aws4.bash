_hexStr() { # data
  local LC_ALL=C
  local i len=${#1} ch
  for (( i = 0; i < len; i++ )); do
    ch=${1:i:1}
    printf '%02x' "'$ch"
  done
}

# _hexStr "asdf"
# _uriEncodeStr "x/xx&x"
# _uriEncodeStr "x/xx&x" 1
# _trimStr $' \n  a\nb\t' | xxd -g 1
# _lowercaseStr "AbCdÄ"
